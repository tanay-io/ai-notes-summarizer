import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export default function ForgotPasswordPage() {
  return (
    <PlaceholderPage
      label="Auth"
      title="Forgot password"
      description="Password reset flow can be added here next. This placeholder keeps the auth navigation complete while preserving the current credentials workflow."
    />
  );
}
